# Pinned-ish minimum versions for the FastAPI sidecar.
# This file is used by `npm run test:backend` and by CI to install the
# Python dependencies before running pytest. Keep the floor versions loose
# (compatible release) so that local dev installs do not break on patch
# upgrades, but pin the major to avoid surprise API changes.

# Web framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0

# SSE streaming used by the training state / param-stats endpoints
sse-starlette>=1.6,<3.0

# File-based state lock shared between the sidecar and the ECGFounder
# training scripts.
filelock>=3.12,<4.0

# Test-only
pytest>=7.4,<9.0
httpx>=0.27,<1.0
